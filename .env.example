# Database connection (for running the server locally alongside a local Postgres)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=capacity
DB_USER=capacity
DB_PASSWORD=

# JWT signing secret — generate with: openssl rand -hex 32
JWT_SECRET=

# Server port (default: 3000)
PORT=3000
