apiVersion: v1
kind: Service
metadata:
  name: {{ include "capacity-review.fullname" . }}
  labels:
    {{- include "capacity-review.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "capacity-review.selectorLabels" . | nindent 4 }}
